<Page
    x:Class="Assignment_UWP_DinhVanHoangAn.Pages.MusicPages.MyMusicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Assignment_UWP_DinhVanHoangAn.Pages.MusicPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:Assignment_UWP_DinhVanHoangAn.Entity"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <ListView x:ConnectionId='8' x:Name="ListViewSong">
                <ListView.ItemTemplate>
                    <DataTemplate                         >
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Height="60" Width="60">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding thumbnail}" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding name}"></TextBlock>
                            <SymbolIcon x:ConnectionId='10' Symbol="Play"                                         Tag="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <!--<StackPanel x:Name="LayoutRoot" VerticalAlignment="Bottom">
                <ProgressBar x:Name="proBar" Value="0" Maximum="200" Margin="0,12" Tapped="ProBar_Tapped" ValueChanged="ProBar_ValueChanged"/>
            --><!--<ProgressBar 
    x:Name="proBar" Minimum="0" 
    Maximum="{Binding NaturalDuration.TimeSpan.TotalSeconds, ElementName=MyMediaElement}" 
    Value="{Binding Position.TotalSeconds, ElementName=MyMediaElement, Mode=OneWay}" />--><!--
            <Slider x:Name="sld">
            </Slider>
        </StackPanel>-->
        <CommandBar VerticalAlignment="Bottom">
            <!--<AppBarToggleButton Icon="Shuffle" Label="Shuffle" Click="AppBarButton_Click" />
            <AppBarToggleButton Icon="RepeatAll" Label="Repeat" Click="AppBarButton_Click"/>-->
            <AppBarSeparator/>
            <AppBarButton x:ConnectionId='3' Icon="Add"  x:Name="btnCreateMusic" Label="Play"                             >Create Music</AppBarButton>
            <AppBarSeparator/>
            <AppBarButton x:ConnectionId='4' Icon="Previous" x:Name="Previous" Label="Previous"                       />
            <AppBarButton x:ConnectionId='5' Icon="Play" x:Name="PlayAndPause" Label="Play"                           />
            <AppBarButton x:ConnectionId='6' Icon="Next" x:Name="Next" Label="Next"                   />

            <!--<CommandBar.SecondaryCommands>
                <AppBarButton Label="Like" Click="AppBarButton_Click"/>
                <AppBarButton Label="Dislike" Click="AppBarButton_Click"/>
            </CommandBar.SecondaryCommands>-->

            <CommandBar.Content>
                <TextBlock x:ConnectionId='7' x:Name="txtNowPlaying" Text="Now playing..." Margin="12,14"/>
            </CommandBar.Content>
        </CommandBar>
        <MediaElement x:ConnectionId='2' x:Name="MyMediaElement"></MediaElement>
    </Grid>
</Page>

